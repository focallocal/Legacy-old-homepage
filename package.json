{
  "name": "fl-home",
  "private": true,
  "scripts": {
    "start": "npm install && meteor run --settings settings.json"
  },
  "dependencies": {
    "jquery": "^3.2.1",
    "meteor-node-stubs": "~0.2.0"
  }
}
